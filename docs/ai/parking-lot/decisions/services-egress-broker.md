---
clyffy_touchpoint:
  version: 2
  workspace_id: wardenclyffe.intelligence
  project_key: services-egress-broker
  persona: clyffy-operator
  kind: runtime
  owner: docs/ai/parking-lot/decisions/services-egress-broker.md
  module: null
  focus_feature: "Building WardenClyffe"
  boundary: services/egress-broker
  domain_status: new
  source_skill: docs/ai/WARDENCLYFFE_PARKING_LOT_SKILL.md
  mesh_registry: wardenclyffe/registry/context-mesh.yaml
  sync:
    qdrant: true
    surreal: true
---

# Parking-Lot Decisions — services-egress-broker

Focus: **Building WardenClyffe**. Boundary: `services/egress-broker`. Domain status is carried in the frontmatter (`domain_status`).

Generated by `scripts/foundation/parking-lot-capture.py`. Projected to Qdrant + SurrealDB by the sync worker (see `docs/ai/TOUCHPOINT_SYNC_PATTERN.md`). Keep entries terse.

<!-- decisions:start -->

<!-- decision:capability-secret-leak-guarantee-broker-at-egress-by-class -->
- **[capability] Secret-leak guarantee (broker-at-egress, by class)** (locked 2026-06-23): Split secrets by class. CLASS A (service/API secrets: Stripe, GitHub, OpenAI, DB passwords) = BROKER-AT-EGRESS: agent holds only an alias; the per-tenant egress broker holds the real token and injects it on the outbound call. Raw secret NEVER enters the workspace fs/env/agent-context. v1 scope = HTTP/S AND TCP (DB/SSH via per-tenant proxy ports) so DB passwords are brokered too, not just API keys. CLASS B (the tool's own subscription tokens: Claude/Codex/Cursor OAuth) = CONTAINED not exposed: short-lived brokered tokens backstopped by default-deny egress (item 4) so a token, even if read, can only reach its own allowlisted endpoint. Principle: no long-lived service secret ever lands in the workspace; subscription tokens contained by egress; fail closed; deterministic (no LLM in the secret path). Infisical stays the source/authority; the egress broker is the consumer-side boundary, upgrading the /run/warden-secrets tmpfs model. Component = NEW boundary services/egress-broker/ (boundary-guard 'new and safe'; operator-approved); sibling to storage-broker-client, distinct concern (credentials vs storage).
<!-- /decision:capability-secret-leak-guarantee-broker-at-egress-by-class -->
<!-- decisions:end -->
