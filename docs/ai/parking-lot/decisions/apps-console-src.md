---
clyffy_touchpoint:
  version: 2
  workspace_id: wardenclyffe.intelligence
  project_key: apps-console-src
  persona: clyffy-operator
  kind: runtime
  owner: docs/ai/parking-lot/decisions/apps-console-src.md
  module: null
  focus_feature: "Building WardenClyffe"
  boundary: apps/console/src
  domain_status: existing
  source_skill: docs/ai/WARDENCLYFFE_PARKING_LOT_SKILL.md
  mesh_registry: wardenclyffe/registry/context-mesh.yaml
  sync:
    qdrant: true
    surreal: true
---


# Parking-Lot Decisions — apps-console-src

Focus: **Building WardenClyffe**. Boundary: `apps/console/src`. Domain status is carried in the frontmatter (`domain_status`).

Generated by `scripts/foundation/parking-lot-capture.py`. Projected to Qdrant + SurrealDB by the sync worker (see `docs/ai/TOUCHPOINT_SYNC_PATTERN.md`). Keep entries terse.

<!-- decisions:start -->
<!-- decision:capability-frontend-architecture-tanstack-start-root-src-domains-dynamic-landing-oklch-rem-coldlight -->
- **[capability] Frontend architecture: TanStack Start, root src/domains, dynamic landing, OKLCH/rem ColdLight** (locked 2026-06-11): Locked 2026-06-11 (operator). (1) Framework = TanStack Start (file-based routing + SSR), replacing the current Vite + hash-routing console; React Aria Components + ColdLight tokens are RETAINED as the component/design foundation under lib/design. (2) Root layout = repo-root 'src/' — NOT 'apps/console/'; relocate apps/console/src -> src/. (3) Domains at 'src/domains/<domain>/<context>/' following the v2 Structure Standard shapes. (4) Styling = OKLCH + rem ColdLight. (5) 'src/domains/landing/' sections load DYNAMICALLY via a strict folder/file/naming convention (route/registry-driven) so adding a section = drop a conventionally-named folder, zero manual wiring. OPEN (must reconcile vs locked separation model before rename/scaffold): operator names domains warden + clyffy + admin + landing, but the repo today has warden + clyffe(customer). Need the canonical domain list — where the customer(clyffe) plane lives, and whether 'admin' = warden or a distinct shell.
<!-- /decision:capability-frontend-architecture-tanstack-start-root-src-domains-dynamic-landing-oklch-rem-coldlight -->
<!-- decision:capability-canonical-frontend-domains-landing-warden-clyffy-admin -->
- **[capability] Canonical frontend domains: landing/warden/clyffy/admin** (locked 2026-06-11): Locked 2026-06-11 (operator). The canonical frontend domains under src/domains/ are EXACTLY four: landing, warden, clyffy, admin. NO top-level clyffe/customer domain for now — the customer surface is deferred or folded into admin later. This REVISES the earlier separation capture (which listed clyffe as a customer plane): the Go clyffe-api customer boundary still stands on the backend, but the FRONTEND domain set is landing/warden/clyffy/admin. Follow-up to honor no-mixing: when the customer view lands inside/under admin, keep operator vs customer concerns separated (role-gated), do not blend them into one view.
<!-- /decision:capability-canonical-frontend-domains-landing-warden-clyffy-admin -->

<!-- decisions:end -->
