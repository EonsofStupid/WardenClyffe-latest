---
clyffy_touchpoint:
  version: 2
  workspace_id: wardenclyffe.intelligence
  project_key: services-warden-api-internal-edge
  persona: clyffy-operator
  kind: runtime
  owner: docs/ai/parking-lot/decisions/services-warden-api-internal-edge.md
  module: null
  focus_feature: "Building WardenClyffe"
  boundary: services/warden-api/internal/edge/
  domain_status: new
  source_skill: docs/ai/WARDENCLYFFE_PARKING_LOT_SKILL.md
  mesh_registry: wardenclyffe/registry/context-mesh.yaml
  sync:
    qdrant: true
    surreal: true
---

# Parking-Lot Decisions — services-warden-api-internal-edge

Focus: **Building WardenClyffe**. Boundary: `services/warden-api/internal/edge/`. Domain status is carried in the frontmatter (`domain_status`).

Generated by `scripts/foundation/parking-lot-capture.py`. Projected to Qdrant + SurrealDB by the sync worker (see `docs/ai/TOUCHPOINT_SYNC_PATTERN.md`). Keep entries terse.

<!-- decisions:start -->

<!-- decision:feature-public-ip-edge-context -->
- **[feature] public-ip-edge-context** (locked 2026-06-13): Public-IP management (add/update/list) lives in a new Go context services/warden-api/internal/edge (matches modules/warden/infrastructure/edge/). New inventory table shippin_infra.public_ips (migration 0007): address inet unique, provider, host_stable_id, role enum ingress|egress|exit|reserved, status enum active|reserved|released, label, note, timestamps + trg set_updated_at. Endpoints under /api/warden/edge/ips: GET list (read), POST add + PATCH {id} update (operator-gated via platform.RequireOperator). The existing shippin_infra.ip_migrations stays as the TRANSITION log (104->204 cutover); public_ips is the INVENTORY it references. Admin UI: src/domains/admin/edge panel + /admin/edge route. Operator-approved naming default 2026-06-13.
<!-- /decision:feature-public-ip-edge-context -->
<!-- decisions:end -->
