---
clyffy_touchpoint:
  version: 2
  workspace_id: wardenclyffe.intelligence
  project_key: modules-shared-contracts
  persona: clyffy-operator
  kind: runtime
  owner: docs/ai/parking-lot/decisions/modules-shared-contracts.md
  module: null
  focus_feature: "Building WardenClyffe"
  boundary: modules/shared/contracts
  domain_status: existing
  source_skill: docs/ai/WARDENCLYFFE_PARKING_LOT_SKILL.md
  mesh_registry: wardenclyffe/registry/context-mesh.yaml
  sync:
    qdrant: true
    surreal: true
---



# Parking-Lot Decisions — modules-shared-contracts

Focus: **Building WardenClyffe**. Boundary: `modules/shared/contracts`. Domain status is carried in the frontmatter (`domain_status`).

Generated by `scripts/foundation/parking-lot-capture.py`. Projected to Qdrant + SurrealDB by the sync worker (see `docs/ai/TOUCHPOINT_SYNC_PATTERN.md`). Keep entries terse.

<!-- decisions:start -->
<!-- decision:capability-clyffy-go-master-modular-update-channels-freeze-restart-version-notify -->
- **[capability] Clyffy-Go master + modular update channels (freeze/restart, version-notify)** (locked 2026-06-11): Locked 2026-06-11. Clyffy-Go is the MASTER version; user Clyffy instances are versioned derivatives bound by contract. On update: users on a given Clyffy version get a NOTIFICATION ahead of time; the update applies through baked-in channels that FREEZE/STOP affected services, apply, then RESTART. Keep updates MODULAR (per-module, minimal blast radius) wherever possible. Contracts live in shared/contracts; align to clyffy-go versioning. Turnkey + documented.
<!-- /decision:capability-clyffy-go-master-modular-update-channels-freeze-restart-version-notify -->
<!-- decision:capability-control-layer-structure-enforcement-constrained-generation-modular-pivot-ready -->
- **[capability] Control-layer structure enforcement — constrained generation (modular, pivot-ready)** (locked 2026-06-11): Locked 2026-06-11 (sourced June-2026 research). Cortex's structure-enforcement plane = constrained generation, kept MODULAR/pivot-ready (fast-evolving field). CONFIRMED: llguidance (Microsoft/guidance-ai, v1.0 Jun-2025, MIT) is real + foundational — token-level CFG/JSON-Schema/regex masks; it is the engine UNDER OpenAI Structured Outputs and integrates with guidance, vLLM, SGLang, llama.cpp, mistral.rs. Peers: XGrammar (Apache-2.0; default in vLLM/SGLang/TensorRT-LLM; best for REPEATED schemas) vs llguidance (best for DYNAMIC/unique-per-request); Outlines = multi-backend framework; guidance = authoring DSL over llguidance. AVOID LMQL + jsonformer (stale). THE SPLIT: self-hosted on Blackwell (vLLM/SGLang, backend=auto) = full token-level constrained decoding; hosted APIs (Claude/Codex) = NATIVE structured outputs — Anthropic JSON Outputs + Strict Tool Use (GA, server-side compiled-grammar constrained sampling, 24h grammar cache, SDK messages.parse->parsed_output); OpenAI response_format (llguidance under the hood). BUT both break schema on REFUSAL + TRUNCATION => ALWAYS post-validate (validation is the guarantee; decode-time constraint is the optimization). PATTERN (the pivot point): a Strategy ConstraintBackend keyed on a portable JSON-Schema/Pydantic IR, with capability negotiation (native decode where supported; else schema-in-prompt + validate/repair) + always-on post-validation; NEVER hardcode an engine into business logic — config-swap like vLLM --structured-outputs-config.backend. Decompose the control layer into 3 swappable planes glued by typed schemas: (1) structure enforcement (ConstraintBackend), (2) context/knowledge routing (MCP), (3) workflow/trigger/function gating (declarative state machine; LangGraph/CrewAI/MS Agent Framework named). PROTOCOLS: MCP (tools/context) + A2A/ACP (agent<->agent); NO token-level steering protocol exists mid-2026 — steering stays a library concern, be skeptical of any 'steering protocol' claim.
<!-- /decision:capability-control-layer-structure-enforcement-constrained-generation-modular-pivot-ready -->
<!-- decision:capability-pulse-packets-signed-modular-update-wire-format-base-schema-kind-overlays -->
- **[capability] .pulse packets — signed modular update wire format (base schema + kind overlays)** (locked 2026-06-11): Locked 2026-06-11 (operator-named '.pulse'). The update channel's wire format: ONE base envelope schema (pulse.packet v1) adjusted per kind. Envelope: pulse_version, uuidv7 id, kind discriminator (app|clyffy|minion), channel (stable|beta|canary), source (master=clyffy-go + master_version + SIGNATURE over payload hash — no unsigned updates), compat (contract_version, applies_from semver range, breaking=>blocked-update-needs-attention), payload (modules[] for minimal blast radius, artifact_ref into W/registry — never inline blobs, sha256, size), lifecycle (notify_window -> freeze[] -> apply[] -> restart[] + rollback_ref backup-before-update + recovery_mode start-paused-on-failure), audit (action_request_id, trace_id). Kind overlays via JSON-Schema if/then: app={routes_rebuild, migrations, assets_ref}; clyffy={persona_delta, skills_delta, memory_boundary_migration, reattune_required => re-run user attunement preflights post-update}; minion={specialist_scope, tool_contract_delta, gateway_reregister}. Publisher = Clyffy-Go master (pushes); consumers = web app, user Clyffy instances, specialist minions subscribe per channel; Clyffy-Dean consumes as ops service, is NOT the master. Validation = envelope JSON-Schema gate + apply-time post-validation (same two-gate pattern as Cortex). NOTE: no standalone Clyffy template scaffold exists in-repo yet; the template is Clyffy-Go on E: — the .pulse contract is authored here in shared/contracts so the template binds to it when ingested.
<!-- /decision:capability-pulse-packets-signed-modular-update-wire-format-base-schema-kind-overlays -->

<!-- decisions:end -->
