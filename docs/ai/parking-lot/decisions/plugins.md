---
clyffy_touchpoint:
  version: 2
  workspace_id: wardenclyffe.intelligence
  project_key: plugins
  persona: clyffy-operator
  kind: runtime
  owner: docs/ai/parking-lot/decisions/plugins.md
  module: null
  focus_feature: "Building WardenClyffe"
  boundary: plugins
  domain_status: new
  source_skill: docs/ai/WARDENCLYFFE_PARKING_LOT_SKILL.md
  mesh_registry: wardenclyffe/registry/context-mesh.yaml
  sync:
    qdrant: true
    surreal: true
---

# Parking-Lot Decisions — plugins

Focus: **Building WardenClyffe**. Boundary: `plugins`. Domain status is carried in the frontmatter (`domain_status`).

Generated by `scripts/foundation/parking-lot-capture.py`. Projected to Qdrant + SurrealDB by the sync worker (see `docs/ai/TOUCHPOINT_SYNC_PATTERN.md`). Keep entries terse.

<!-- decisions:start -->

<!-- decision:capability-w-rooted-plugin-template-cortex-control-cortex-intelligence-packages -->
- **[capability] W-rooted plugin template — cortex-control + cortex-intelligence packages** (locked 2026-06-12): Locked 2026-06-12 (operator). Plugins are TURNKEY, TEMPLATE-SHAPED packages browsable on the W drive: repo plugins/ is the source; cloned to W:/plugins per the W rule so the SAME package is reachable from local workstation and devstation. Template shape per plugin: plugin.json (name, slug, kind control|intelligence|minion, version, mcp_id, entry{command,args}, requires{secret REFS only, network}), connect/claude-desktop.json + connect/codex.toml + connect/claude-code.sh (copyable snippets), README.md. TWO first plugins: (1) cortex-control = the control layer (PR1 catalog: commit_decision/validate/search/list_plugins + law resources + strict prompts); (2) cortex-intelligence = DIRECT connection to the intelligence plane — self-hosted Qdrant (LXC 106) + self-hosted SurrealDB (LXC 104; Surreal-Cloud->self-hosted migration is the locked direction) — read-only v1 tools surreal.query (mutation-guarded SELECT/INFO only), surreal.list_tables, qdrant.list_collections, qdrant.collection_info, qdrant.scroll; creds NEVER in the package — env/Infisical refs materialized at /run/warden-secrets. ONE binary serves both: cortex-mcp [control|intelligence] — two packages, zero code duplication, future minions reuse the same template. mcp_id for intelligence: mcp.workspace.clyffy-master.intelligence (scaffold; registry entry + ADR 0030 bar before formal).
<!-- /decision:capability-w-rooted-plugin-template-cortex-control-cortex-intelligence-packages -->
<!-- decisions:end -->
