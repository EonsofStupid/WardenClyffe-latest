---
clyffy_touchpoint:
  version: 2
  workspace_id: wardenclyffe.intelligence
  project_key: modules-warden-infrastructure-devstation-turnkey
  persona: clyffy-operator
  kind: runtime
  owner: docs/ai/parking-lot/decisions/modules-warden-infrastructure-devstation-turnkey.md
  module: null
  focus_feature: "Building WardenClyffe"
  boundary: modules/warden/infrastructure/devstation/turnkey/
  domain_status: existing
  source_skill: docs/ai/WARDENCLYFFE_PARKING_LOT_SKILL.md
  mesh_registry: wardenclyffe/registry/context-mesh.yaml
  sync:
    qdrant: true
    surreal: true
---


# Parking-Lot Decisions — modules-warden-infrastructure-devstation-turnkey

Focus: **Building WardenClyffe**. Boundary: `modules/warden/infrastructure/devstation/turnkey/`. Domain status is carried in the frontmatter (`domain_status`).

Generated by `scripts/foundation/parking-lot-capture.py`. Projected to Qdrant + SurrealDB by the sync worker (see `docs/ai/TOUCHPOINT_SYNC_PATTERN.md`). Keep entries terse.

<!-- decisions:start -->
<!-- decision:capability-devstation-secret-pipeline-turnkey -->
- **[capability] devstation-secret-pipeline-turnkey** (locked 2026-06-13): The no-leak secret pipeline is built turnkey under modules/warden/infrastructure/devstation/turnkey/: a systemd infisical-agent.service runs 'infisical agent --config /etc/warden/agent-config.yaml' (sourced format, CLI 0.43.x: universal-auth reading root-only client-id/client-secret files, sink token + template rendering ALL project secrets to /run/warden-secrets/warden.env via the listSecrets DSL, polling-interval refresh). install-devstation-turnkey.sh installs units + derives creds from /etc/warden/infisical-mi.env (root-only 0600). warden-devstation-status verifies agent-active + token + warden.env + DB + W-mount + plugins-bin and exits non-zero on any fail. DIAGNOSIS 2026-06-13: on warden-devstation-01 the agent was NEVER installed (no unit, no /etc/warden/infisical-mi.env, /run/warden-secrets empty) and the user-copy MI client secret returns 401 (expired/rotated). egress IP 104.176.44.101 confirmed. OPERATOR GATE: issue a live Universal Auth client secret (and confirm Trusted-IP allowlist) so the agent authenticates.
<!-- /decision:capability-devstation-secret-pipeline-turnkey -->
<!-- decision:capability-secret-access-authority-is-w-configuration -->
- **[capability] secret-access-authority-is-w-configuration** (locked 2026-06-14): REALIGNMENT 2026-06-14: the single authority for all AI/devstation access is W:/configuration (authority.yaml + providers/{infisical,runtime-keyring,ssh,github}.yaml + clients/{claude,codex}.yaml + agents/clyffy.yaml), surfaced by ACCESS_AND_SECRETS.md / CONFIGURATION.md and the registry workspace.runtime.yaml + configuration.inventory.json. The installed pattern: warden-infisical-bootstrap encrypts the MI client id/secret with systemd-creds into /etc/credstore.encrypted (0600 root); warden-infisical-agent.service LoadCredentialEncrypted -> /run/credentials -> infisical agent -> token sink /run/warden-secrets/infisical-access-token; warden-infisical-status reports the state machine (…credentials_missing -> …active); warden-devstation-status reports host/workspace/tools/git; runtime keyring /run/warden-secrets (0700, reboot-cleared) via warden-secret-* helpers; secret_values_on_w forbidden; static_pat break-glass only. SUPERSEDES the divergent turnkey pipeline I authored (plaintext /etc/warden/infisical-mi.env + my own agent-config + install-devstation-turnkey.sh + infisical-agent.service + warden-secrets-preflight/refresh + a duplicate warden-devstation-status) — DELETED. turnkey/ now holds a verbatim mirror of the installed authority for turnkey replication. Connect & Launch (/admin/connect, internal/connect) DRIVES the authority: status via warden-infisical-status, activate via warden-infisical-bootstrap (stdin id+secret), never a parallel pattern. GitHub added to the schema (providers/github.yaml): durable path = Infisical-brokered PAT to /run/warden-secrets/github-pat; static PAT break-glass only. Only the live MI client secret is missing (status: machine_identity_credentials_missing).
<!-- /decision:capability-secret-access-authority-is-w-configuration -->

<!-- decisions:end -->
