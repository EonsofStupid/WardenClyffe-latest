---
clyffy_touchpoint:
  version: 2
  workspace_id: wardenclyffe.intelligence
  project_key: modules-warden-interfaces-mcp
  persona: clyffy-operator
  kind: runtime
  owner: docs/ai/parking-lot/decisions/modules-warden-interfaces-mcp.md
  module: null
  focus_feature: "Building WardenClyffe"
  boundary: modules/warden/interfaces/mcp
  domain_status: existing
  source_skill: docs/ai/WARDENCLYFFE_PARKING_LOT_SKILL.md
  mesh_registry: wardenclyffe/registry/context-mesh.yaml
  sync:
    qdrant: true
    surreal: true
---



# Parking-Lot Decisions — modules-warden-interfaces-mcp

Focus: **Building WardenClyffe**. Boundary: `modules/warden/interfaces/mcp`. Domain status is carried in the frontmatter (`domain_status`).

Generated by `scripts/foundation/parking-lot-capture.py`. Projected to Qdrant + SurrealDB by the sync worker (see `docs/ai/TOUCHPOINT_SYNC_PATTERN.md`). Keep entries terse.

<!-- decisions:start -->
<!-- decision:capability-w-drive-general-projects-mcp-mcp-global-projects -->
- **[capability] W-drive general projects MCP (mcp.global.projects)** (locked 2026-06-10): Locked 2026-06-10 (operator-approved name mcp.global.projects). A general MCP rooted on the W drive (primary drive) that discusses the active projects devpulse, wardenclyffe, clyffy-go. Source: all three are moved/mirrored onto W via the Clyffy-dean E->W process first; the MCP reads only from W. Scope: read-only context/retrieval (summaries, docs, decisions, status, relationships from Qdrant/SurrealDB + a project registry) PLUS read access to project source files on W, gated by a secrets-exclusion guard so no API keys/secrets are surfaced (devstation VM-safety). Topology: registered as a focused L2 leaf under mcp.workspace.clyffy-master-gateway per the federation model; must meet the May 2026 MCP production bar (ADR 0030) when formalized.
<!-- /decision:capability-w-drive-general-projects-mcp-mcp-global-projects -->
<!-- decision:capability-cortex-control-intelligence-exposed-to-all-ai-tools-via-one-mcp-gateway -->
- **[capability] Cortex — control+intelligence exposed to all AI tools via one MCP gateway** (locked 2026-06-11): Locked 2026-06-11 (sourced 2026 best-practice research). CORTEX = the unifying backbone over the CONTROL layer (Warden Go API + policy) and the INTELLIGENCE layer (Postgres truth + SurrealDB graph + Qdrant retrieval + Markdown touchpoints), exposed to every AI tool through ONE MCP gateway. MCP is the cross-tool standard — Claude Code, OpenAI Codex CLI, and Claude Desktop all consume MCP; the gateway MUST serve Streamable HTTP (Codex requirement) + OAuth 2.1 (already our ADR 0030 bar; this IS the existing mcp.workspace.clyffy-master-gateway). PRIMITIVE MAPPING: TOOLS = control-layer callable functions (query graph, validate boundary, commit decision, search) — LLM-invoked, each with a JSON-Schema inputSchema; RESOURCES = intelligence read-context (schemas, touchpoints, locked decisions) — host-selected; PROMPTS = the STRICT step-by-step processes (planning/parking-lot, boundary-guard, build-spec, review) — the portable cross-tool way to ship a strict process. HARD SCHEMAS: tool inputSchema (enforced) + Structured Outputs (Pydantic/Zod) on the final response validated against the canonical v2 clyffy_touchpoint schema => every capture is a valid domain MEMORY (commit_decision tool -> versioned touchpoint -> Surreal/Qdrant). DISTRIBUTION: Claude Code = a PLUGIN bundling the gateway .mcp.json + Agent Skills (procedure wrappers) + hooks (auto-capture/sync), install-once auto-wired; Codex = MCP config in ~/.codex/config.toml (Streamable HTTP), NO skills/hooks so procedures live as MCP PROMPTS + tools; Claude Desktop = MCP via Settings/Extensions or a .mcpb bundle. CROSS-TOOL RULE (extends our authored-once/wrapped-per-agent pattern): author each strict process ONCE as an MCP prompt (portable) + thin Claude Code skill/hook wrapper; skills+hooks are Claude-Code-only, never the sole home of a process. Flags: SSE deprecated (Streamable HTTP per ADR 0030); CC plugins ship .mcp.json (auto-load) but do NOT auto-install MCP deps; Agent SDK separate credit from 2026-06-15.
<!-- /decision:capability-cortex-control-intelligence-exposed-to-all-ai-tools-via-one-mcp-gateway -->
<!-- decision:feature-cortex-control-layer-build-spec-authored-cortex-control-layer-spec -->
- **[feature] Cortex control-layer build spec authored (CORTEX_CONTROL_LAYER_SPEC)** (locked 2026-06-11): Locked 2026-06-11. docs/specs/CORTEX_CONTROL_LAYER_SPEC.md is the build spec for the Cortex plugin + surfaces. Boundaries locked: services/cortex-mcp (Go MCP server, stdio-first, tools cortex.commit_decision/validate_touchpoints/search_decisions/list_plugins + resources + prompts planning-workflow/boundary-guard-checklist/build-spec); warden-api mesh endpoints (/api/warden/mesh/plugins, /plugins/{id}/connect, /intelligence); admin views ControlLayerView + IntelligenceLayerView (admin = shell domain, composes other domains' svc — explicit mirroring exception); clyffy home wired to /api/clyffy/*; plugins/cortex Claude Code bundle (.mcp.json auto-wire) + registry entry mcp.workspace.clyffy-master.cortex (scaffold -> formal-mcp only after ADR 0030 bar). Connect = stdio-over-SSH snippet until Streamable HTTP phase 2. Acceptance: operator adds Cortex to Claude Desktop/Codex/Claude Code and sees tools; /admin shows control + intelligence layers with active plugins + copyable connect snippets. Build order PR1-PR4.
<!-- /decision:feature-cortex-control-layer-build-spec-authored-cortex-control-layer-spec -->

<!-- decisions:end -->
