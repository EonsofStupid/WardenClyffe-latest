---
clyffy_touchpoint:
  version: 2
  workspace_id: wardenclyffe.intelligence
  project_key: services-warden-api-internal-connect
  persona: clyffy-operator
  kind: runtime
  owner: docs/ai/parking-lot/decisions/services-warden-api-internal-connect.md
  module: null
  focus_feature: "Building WardenClyffe"
  boundary: services/warden-api/internal/connect/
  domain_status: new
  source_skill: docs/ai/WARDENCLYFFE_PARKING_LOT_SKILL.md
  mesh_registry: wardenclyffe/registry/context-mesh.yaml
  sync:
    qdrant: true
    surreal: true
---

# Parking-Lot Decisions — services-warden-api-internal-connect

Focus: **Building WardenClyffe**. Boundary: `services/warden-api/internal/connect/`. Domain status is carried in the frontmatter (`domain_status`).

Generated by `scripts/foundation/parking-lot-capture.py`. Projected to Qdrant + SurrealDB by the sync worker (see `docs/ai/TOUCHPOINT_SYNC_PATTERN.md`). Keep entries terse.

<!-- decisions:start -->

<!-- decision:feature-connect-launch-auth-surface -->
- **[feature] connect-launch-auth-surface** (locked 2026-06-13): Connect & Launch is the devstation click-to-auth surface: Go context services/warden-api/internal/connect (GET /api/warden/connect/status = per-tool auth state; POST /api/warden/connect/infisical = accept a LIVE MI client secret or PAT, write root-only /etc/warden/infisical-mi.env via sudo + run install-devstation-turnkey.sh + return warden-devstation-status; POST /api/warden/connect/github = accept a GitHub PAT, configure git credential helper, verify via ls-remote). All mutating routes operator-gated (platform.RequireOperator); secrets NEVER logged, written only to root-only paths / git credential store. Admin UI src/domains/admin/connect ConnectView + route /admin/connect. DIAGNOSIS 2026-06-13 (exhaustive): no PAT or Infisical session exists on warden-devstation-01 ('No valid login session found'); MI client secret returns 401 (expired). This panel is the supply point for the one live credential only the operator holds.
<!-- /decision:feature-connect-launch-auth-surface -->
<!-- decisions:end -->
