---
clyffy_touchpoint:
  version: 2
  workspace_id: wardenclyffe.intelligence
  project_key: docs-wardenclyffe-buildplan-md
  persona: clyffy-operator
  kind: runtime
  owner: docs/ai/parking-lot/decisions/docs-wardenclyffe-buildplan-md.md
  module: null
  focus_feature: "Building WardenClyffe"
  boundary: docs/WARDENCLYFFE_BUILDPLAN.md
  domain_status: existing
  source_skill: docs/ai/WARDENCLYFFE_PARKING_LOT_SKILL.md
  mesh_registry: wardenclyffe/registry/context-mesh.yaml
  sync:
    qdrant: true
    surreal: true
---



# Parking-Lot Decisions — docs-wardenclyffe-buildplan-md

Focus: **Building WardenClyffe**. Boundary: `docs/WARDENCLYFFE_BUILDPLAN.md`. Domain status is carried in the frontmatter (`domain_status`).

Generated by `scripts/foundation/parking-lot-capture.py`. Projected to Qdrant + SurrealDB by the sync worker (see `docs/ai/TOUCHPOINT_SYNC_PATTERN.md`). Keep entries terse.

<!-- decisions:start -->
<!-- decision:capability-wardenclyffe-buildplan-locked-p0-p3-interleaved-own-migrations-runner -->
- **[capability] WardenClyffe buildplan locked (P0-P3, interleaved, own migrations runner)** (locked 2026-06-12): Locked 2026-06-12. docs/WARDENCLYFFE_BUILDPLAN.md is the execution record. Operator rulings: plan home = WARDENCLYFFE_BUILDPLAN.md; sequencing = INTERLEAVED (every phase ships a visible slice — P0's is the /admin Control+Intelligence panels, Cortex PR2/PR3 pulled forward); migrations = OWN tiny Go runner (services/warden-api/cmd/warden-migrate over data/schema/sql + warden_core.schema_migrations); branch = commit foreign WIP as-is under its own label, then merge codex/warden-devstation-hosted-vscode -> main behind the new CI gate. P0: CI gate, migrations runner, identity+RLS build w/ hades seed, admin panels, merge; operator key-turns in parallel (Infisical gate, PG on Warden VM). P1: sync worker v1, PG cutover, backups+restore drill, golden template. P2: Connect&Launch, chat boundary, OIDC, .pulse v1. P3: E->W clones/ColdLight/MakersImpulse, v1->v2 sweep + api.ts split, ADR0030 bar, ConstraintBackend. Non-goals until customer #2: k8s, NATS, ClickHouse, multi-region, L0 federation publish.
<!-- /decision:capability-wardenclyffe-buildplan-locked-p0-p3-interleaved-own-migrations-runner -->
<!-- decision:capability-alpha-definition-of-done-naming-involvement-colocation-complete-approved-names -->
- **[capability] Alpha definition-of-done + naming involvement + colocation-complete + approved names** (locked 2026-06-12): Locked 2026-06-12 (operator). (1) NAMING INVOLVEMENT: the operator is involved in vars/naming — new names (vars, files, services, schemas) are presented for approval before use, aligning to operator taste; baked into the build-spec prompt. (2) ALPHA DEFINITION OF DONE (the 'truly established, move on' bar, recorded in WARDENCLYFFE_BUILDPLAN): login+role-switch live (subjects+RLS, hades seeded); CI green on main; warden-migrate rebuilds from zero; BOTH plugins connected in Claude Desktop with LIVE intelligence data; /admin shows both layers w/ active plugins + connect snippets; sync worker projecting; one restore drill passed; every spec carries a Minimal Requirements section. (3) COLOCATION-COMPLETE rule in the structure standard: a context ships complete (types.ts + <ctx>.svc.ts + components/ + views/ + index.ts, utils colocated) or not at all — no file spray, no half-contexts; React stays dumb, driven by Go. (4) Operator-approved names: services/warden-api/cmd/warden-migrate (migrations runner), services/intelligence-sync (sync worker), schemas/pulse/pulse-packet.v1.schema.json (.pulse home).
<!-- /decision:capability-alpha-definition-of-done-naming-involvement-colocation-complete-approved-names -->
<!-- decision:capability-buildplan-second-pass-schema-code-finds-sourced-integration-pins -->
- **[capability] Buildplan second pass: schema/code finds + sourced integration pins** (locked 2026-06-12): Locked 2026-06-12 (second pass per process law; what it changed): (1) P0-2 SIMPLER — warden_core.schema_migrations ledger already exists in 0001 and migrations self-register; runner = dir-diff-apply. (2) P0-3 CORRECTED — warden_core.subjects already exists in 0001 (kind enum, external_id=Authentik sub); migration 0006 EXTENDS it (citext email, email_verified_at, status, subject_tenant+role, identity.* fns, trg_*, RLS, uuidv7 new rows), not green-field; IDENTITY_TENANCY_SPEC's 'nothing exists yet' is stale on this point. (3) Storage context verified REAL: storage-broker-client Go control seam + Rust wardenclyffedisk data plane tested end-to-end (memory store); its POA&M (Postgres store, provisioner wiring, retire SMB) interlocks with P1/P2. (4) OFFICIAL_BASELINE apps/-anchored frontend section superseded in place (root src/, operator 2026-06-11); its data-plane topology + token pipeline remain authoritative. (5) Sourced integration pins locked: go-oidc v3.18.0 (Authentik issuer slug-URL trailing slash; groups via profile scope), pgx >=5.9.2 security floor (have 5.10.0), tx-scoped set_config for RLS + non-owner app role, surrealdb.go v1.4.0, qdrant go-client v1.18.2 (gRPC 6334), MCP go-sdk v1.6.1 GA for phase-2 transport swap (Go 1.25+), setup-go@v6 go-version-file:go.work + cache-dependency-path **/go.sum, single Go job + parallel npm.
<!-- /decision:capability-buildplan-second-pass-schema-code-finds-sourced-integration-pins -->

<!-- decisions:end -->
