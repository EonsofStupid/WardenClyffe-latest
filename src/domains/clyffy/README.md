# domain: clyffy

The assistant / orchestrator surface (`clyffy.ai`). Persona only — it does **not**
hold product authority (that is `warden`) and is **not** the customer plane.
Reads through `/api/clyffy/*`. Keep contexts here named identically to their Go
counterparts per the Structure Standard.
