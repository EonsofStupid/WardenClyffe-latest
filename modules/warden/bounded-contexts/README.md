# Warden Bounded Contexts

Each directory here owns one Warden domain language. Contexts communicate
through explicit contracts, not shared mutable assumptions.

Start with `proxmox`, `fleet`, `edge`, `dns`, and `mesh`; those unlock the
host/server panel, public homebase, domains, and MCP observatory.
