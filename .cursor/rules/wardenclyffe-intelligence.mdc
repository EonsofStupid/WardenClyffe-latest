---
description: WardenClyffe base skill, agent touchpoints, and MCP mesh routing.
alwaysApply: true
---

# WardenClyffe Intelligence Rule

Before making repo changes, read `AGENTS.md`.

Use these source files for project-wide agent behavior:

- `docs/ai/WARDENCLYFFE_BASE_SKILL.md`
- `docs/ai/MCP_MESH_TOUCHPOINTS.md`
- `docs/ai/INTELLIGENCE_TOUCHPOINTS.md`
- `docs/WARDEN_CLYFFE_ARCHITECTURE.md`
- `docs/WARDEN_CLYFFE_PILOT_ROADMAP.md`

The current working Context Mesh/MCP registry lives at
`wardenclyffe/registry/context-mesh.yaml`. Do not invent parallel MCP
tool names or endpoint contracts in Cursor rules.

Preserve the product split:

- Warden is the operator/server-control product.
- Clyffe is the customer portal, knowledge base, tickets, CRM, and
  tenant-safe service panel.

Never include secrets in generated files or chat-visible outputs.

